# Streamlit Dashboard Assets

This folder contains the static dashboard files embedded by `streamlit_app/app.py`.

Keep these files in sync with `outputs/dashboard-public` when the local dashboard UI changes:

- `index.html`
- `style.css`
- `app.js`
